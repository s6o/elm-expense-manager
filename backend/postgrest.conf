db-uri = "postgres://postgres:postgres@localhost:5432/elm_expense_manager"
db-schema = "api"
db-anon-role = "anon"
db-pool = 1

server-host = "*4"
server-port = 3333

## base url for swagger output
# server-proxy-uri = ""

## choose a secret to enable JWT auth
## (use "@filename" to load from separate file)
# jwt-secret = "<for-auth0>"
# secret-is-base64 = true

## limit rows in response
# max-rows = 1000

## stored proc to exec immediately after auth
pre-request = "api.check_auth"
